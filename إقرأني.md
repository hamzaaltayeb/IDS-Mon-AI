# 🛡️ نظام مراقبة أمن الشبكات الذكي بالذكاء الاصطناعي (AI-NSMS)

منصة متكاملة لإدارة العمليات الأمنية (SOC Platform) في الوقت الفعلي مدعومة بنماذج الذكاء الاصطناعي وتعلم الآلة لتحليل تدفقات الشبكة واكتشاف الأنشطة والهجمات السيبرانية وتصنيفها آلياً.

---

## 🎯 نطاق الهجمات المستهدفة
1. **هجمات حجب الخدمة (DDoS)**: درجة التهديد `95/100` - مستوى الخطورة: **حرج (CRITICAL)**.
2. **استطلاع وفحص المنافذ (Port Scan)**: درجة التهديد `70/100` - مستوى الخطورة: **عالي (HIGH)**.
3. **هجمات التخمين وكسر التوثيق (Brute Force)**: درجة التهديد `85/100` - مستوى الخطورة: **عالي (HIGH)**.
4. **حركة الشبكة الطبيعية (Normal Traffic)**: درجة التهديد `0/100` - مستوى الخطورة: **منخفض (LOW)**.
5. **الأنشطة المجهولة والشاذة (Unknown Anomalies)**: درجة التهديد `50/100` - مستوى الخطورة: **متوسط (MEDIUM)**.

---

## 📊 الخصائص الـ 11 المستخرجة من كل تدفق شبكي (11 Network Flow Features)
1. `Protocol` (بروتوكول النقل مثل TCP أو UDP)
2. `Destination Port` (رقم منفذ الوجهة)
3. `Total Flow Size` (إجمالي حجم التدفق بالبايت)
4. `Average Packet Size` (متوسط حجم الحزمة)
5. `Standard Deviation of Packet Size` (الانحراف المعياري لحجم الحزم)
6. `Packet Count` (إجمالي عدد الحزم في التدفق)
7. `Flow Duration` (مدة التدفق بالثواني)
8. `Average Inter-Arrival Time` (متوسط الوقت بين وصول الحزم)
9. `Maximum Inter-Arrival Time` (أقصى وقت بين وصول الحزم)
10. `Packets Per Second` (معدل الحزم في الثانية)
11. `Bytes Per Second` (معدل البايتات في الثانية)

---

## 🗄️ قاعدة البيانات التشغيلية ودعم PostgreSQL
يحتوي النظام على ملف المخطط [`schema.sql`](file:///home/hamza/Desktop/Intelligent-Network-Security-Monitoring-System-using-AI-main/schema.sql) الذي يحتوي على 5 جداول مترابطة:
- `users`: المستخدمون، أدوار RBAC (`ADMIN` و `SECURITY_ANALYST`)، تشفير كلمات المرور.
- `network_flows`: سجلات الخصائص الـ 11 للتدفقات (لا يتم تخزين الحزم الخام).
- `detections`: نتائج كشف الذكاء الاصطناعي، درجة الثقة، Threat Score، والخطورة، ومؤشرات التحليل الجنائي.
- `alerts`: طابور التنبيهات الأمنية التفاعلي (`NEW`, `ACKNOWLEDGED`, `RESOLVED`).
- `system_logs`: سجل تدقيق غير قابل للتعديل لجميع أنشطة المسؤولين والمحللين.

### تهيئة PostgreSQL (اختياري / بيئة الإنتاج):
```bash
# 1. إنشاء قاعدة البيانات
createdb ids_database

# 2. استيراد المخطط
psql -d ids_database -f schema.sql

# 3. تمرير متغير البيئة
export DATABASE_URL="postgresql://postgres:password@localhost:5432/ids_database"
```
*(إذا لم يتم تمرير المتغير، سيعمل النظام تلقائياً وبشكل فوري على SQLite المحلية `data/ids_database.db` دون الحاجة لأي إعداد إضافي)*.

---

## 👥 حسابات الاختبار الافتراضية
| اسم المستخدم | كلمة المرور | الدور (Role) | الصلاحيات |
|---|---|:---:|---|
| `admin` | `Admin@12345` | **ADMIN** | وصول كامل: لوحة التحكم، التدفقات، الكشوفات، التنبيهات، التقارير، إدارة المستخدمين، سجلات التدقيق |
| `analyst` | `Analyst@12345` | **SECURITY_ANALYST** | صلاحيات تشغيلية: لوحة التحكم، التدفقات، الكشوفات، إدارة التنبيهات، التقارير (محجوب من إدارة المستخدمين) |

---

## 🚀 التشغيل السريع

### 1. تفعيل البيئة الافتراضية وتثبيت المتطلبات:
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### 2. تدريب نماذج الذكاء الاصطناعي على بيانات UNSW-NB15 الحقيقية:
```bash
python src/train_model.py
```

### 3. تشغيل لوحة تحكم العمليات الأمنية (SOC Portal):
```bash
python src/dashboard.py
```
افتح المتصفح على: **`http://localhost:5000`**

---

## 🌐 أوضاع المراقبة المدعومة (Dual Monitoring Modes)

يدعم النظام وضعين تشغيليين منفصلين تماماً:

### 1. وضع المحاكاة (Simulation Mode):
- يقرأ عينات تدفقات حقيقية من ملف الاختبار المعياري `UNSW-NB15` ويمررها لخط الفحص والتصنيف (مفيد للاختبارات المعملية والتطوير دون الحاجة لصلاحيات Root):
```bash
python src/realtime_monitor.py --mode simulation -n 10
```

### 2. وضع المراقبة الحية الفعلي (Live Network Monitoring Mode):
- يلتقط الحزم الفعلية من واجهة الشبكة المختارة (`eth0`, `wlan0`, `lo`, إلخ).
- يجمع الحزم في تدفقات بناءً على المفتاح الخماسي، ويستخرج الخصائص الـ 11، ويمررها لنموذج الذكاء الاصطناعي ويسجل النتائج والتنبيهات في قاعدة البيانات:
```bash
sudo ./venv/bin/python src/realtime_monitor.py --mode live --interface wlan0 --flow-timeout 10 --filter "ip"
```

---

## 🧪 سيناريوهات الاختبار المعملي (Local Lab Testing)

1. **حركة تصفح طبيعية (Normal Traffic):** تصفح مواقع ويب HTTPS/TCP (تصنف كـ `NORMAL` ودرجة تهديد `0`).
2. **فحص المنافذ المصرح به (Port Scan):**
   ```bash
   nmap -sS -p 20-1000 192.168.1.50
   ```
   *(تصنف كـ `PORT_SCAN`، درجة التهديد: `70/100`، الخطورة: `HIGH`)*
3. **محاولات كسر التوثيق (SSH Brute Force):**
   ```bash
   hydra -l admin -P wordlist.txt ssh://192.168.1.50
   ```
   *(تصنف كـ `BRUTE_FORCE`، درجة التهديد: `85/100`، الخطورة: `HIGH`)*
4. **محاكاة هجمات حجب الخدمة (DDoS / SYN Flood):**
   ```bash
   hping3 -S --flood -p 80 192.168.1.50
   ```
   *(تصنف كـ `DDOS`، درجة التهديد: `95/100`، الخطورة: `CRITICAL`)*

---

## 🎓 التمييز الأكاديمي وحدود النظام

> [!IMPORTANT]
> **ملاحظة منهجية لمشروع التخرج:**
> 1. بيانات **UNSW-NB15** استُخدمت حصراً لتدريب نماذج الذكاء الاصطناعي وضبط المعاملات.
> 2. **وضع المراقبة الحية (Live Mode)** يلتقط حركة البيانات الحقيقية من واجهات الشبكة ويستخرج الخصائص الـ 11 عبر `FlowCollector`.
> 3. يقوم `FeatureCompatibilityAdapter` بمواءمة الخصائص المستخرجة حياً مع مصفوفة الإدخال للنماذج لضمان عمل التصنيف دون أي تصنيع للبيانات.


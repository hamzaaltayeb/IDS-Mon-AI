# 🛡️ نظام مراقبة أمن الشبكات الذكي باستخدام الذكاء الاصطناعي (AI-NSMS)

منصة متكاملة لمركز العمليات الأمنية (SOC Platform) مدعومة بالذكاء الاصطناعي وتعلم الآلة للمراقبة اللحظية لتدفقات الشبكة، واكتشاف الشذوذ الإحصائي، والتصنيف التلقائي للتهديدات والهجمات السيبرانية.

---

## 🎯 تصنيف التهديدات المستهدفة في النظام

| فئة التهديد | مستوى الخطورة | درجة التهديد الأساسية | البصمة الهجومية والسلوكية |
|---|:---:|:---:|---|
| **هجمات حجب الخدمة (DDoS)** | `CRITICAL` | `95 / 100` | تدفقات حجمية هائلة، تشبع في معدل الحزم ($\ge 1500\text{ pps}$)، استهلاك النطاق الترددي |
| **فحص المنافذ (Port Scanning)** | `HIGH` | `70 / 100` | مسح استطلاعي لعدة منافذ مختلفة من نفس المصدر ($\ge 10\text{ ports}$) في نافذة زمنية قصيرة |
| **كسر التوثيق (Brute Force)** | `HIGH` | `85 / 100` | محاولات متكررة ومكثفة لكسر كلمات المرور على منافذ التوثيق (SSH `22`, FTP `21`, RDP `3389`, DBs) |
| **التهديدات المجهولة (Unknown Threat)** | `MEDIUM` | `50 / 100` | انحرافات وشذوذ إحصائي يرصده `Isolation Forest` ($\text{score} \le -0.05$) بتوزيع غير مألوف |
| **حركة المرور الطبيعية (Normal)** | `LOW` | `0 / 100` | اتصالات عادية مشروعة متوافقة مع خط الأساس للشبكة (تصفح، استعلامات DNS، تحديثات) |

---

## 🔬 بنية الكشف الذكية متعددة المستويات (Multi-Tiered AI Architecture)

يعتمد نظام AI-NSMS على بنية كشف دفاعية متعددة الطبقات لمنع التنبيهات الزائفة على حركة المرور الطبيعية مع الحفاظ على حساسية عالية للهجمات الحقيقية:

```text
[ واجهة الشبكة الفعلية (wlp108s0 / eth0 / lo) ]
                         ↓
[ لاقط الحزم اللحظي باستخدام Scapy (Promiscuous Packet Sniffer) ]
                         ↓
[ جدول تجميع الجلسات المتناظر ثنائي الاتجاه (Canonical Bidirectional FlowTable) ]
                         ↓
[ استخراج الخصائص الـ 11 للتدفق مع الحماية الرياضية للحزم الفردية ]
                         ↓
[ المعايرة والتطبيع الرياضي القياسي (StandardScaler) ]
                         ↓
       ┌─────────────────┴─────────────────┐
       ▼                                   ▼
[ مصنف الغابات العشوائية متعدد الفئات ]   [ نموذج كشف الشذوذ Isolation Forest ]
  (احتماليات حقيقية معايرة بدقة)            (مؤشر الشذوذ الإحصائي Inlier/Outlier)
       └─────────────────┬─────────────────┘
                         ↓
[ محرك التحكيم واتخاذ القرار متعدد المستويات (pipeline.py) ]
 • المستوى 1: أرضيات الأمان الحجمية (DDoS: ≥ 50 حزمة و ≥ 50 كيلوبايت و ≥ 1500 pps)
 • المستوى 2: التحكيم بالاحتمالية المعايرة ($P(\text{Attack}) \ge 0.55$)
 • المستوى 3: عزل الشذوذ الإحصائي غير المحدد وتصنيفه كـ UNKNOWN
 • المستوى 4: خط الأساس الطبيعي (Threat Score = 0/100 ومنع التنبيهات الزائفة)
                         ↓
[ مصفوفة حساب درجة التهديد الشارحة (0–100) وتوليد المؤشرات الجنائية ]
                         ↓
[ منع التكرار والتخزين في قاعدة البيانات ]
 • network_flows: تسجيل 100% من تدفقات الشبكة لعرض التليمترية والمخططات.
 • detections: تسجيل كافة التصنيفات (طبيعي، مجهول، تهديد، هجوم).
 • alerts: إنشاء التنبيهات فقط للتهديدات المؤكدة مع نافذة منع تكرار (15 ثانية).
                         ↓
[ واجهات برمجية RESTful API وبث التليمترية اللحظي SSE ]
                         ↓
[ لوحة التحكم الأمنية (SOC Dashboard) ووحدة المراقبة اللحظية ]
```

---

## 📊 الخصائص الـ 11 المستخرجة من تدفقات الشبكة

1. **`Protocol`**: بروتوكول النقل (`TCP`, `UDP`, `ICMP`, `OTHER`).
2. **`Destination Port`**: منفذ الخدمة المستهدف (مثل `80`, `443`, `53`, `22`).
3. **`Total Flow Size`**: إجمالي حجم البيانات المنقولة بالبايت في الجلسة.
4. **`Average Packet Size`**: متوسط حجم الحزمة في التدفق ($\text{bytes} / \text{packets}$).
5. **`Standard Deviation of Packet Size`**: الانحراف المعياري لأحجام الحزم.
6. **`Packet Count`**: إجمالي عدد الحزم المتبادلة في الاتجاهين.
7. **`Flow Duration`**: مدة الجلسة الفعالة بالثواني ($t_{\text{last}} - t_{\text{first}}$).
8. **`Average Inter-Arrival Time`**: متوسط الفاصل الزمني بين الحزم المتتالية.
9. **`Maximum Inter-Arrival Time`**: أقصى فاصل زمني بين حزمتين.
10. **`Packets Per Second (PPS)`**: معدل الحزم في الثانية.
11. **`Bytes Per Second (BPS)`**: معدل نقل البيانات بالبايت في الثانية.

---

## 👥 حسابات الدخول الافتراضية لمنصة SOC

| اسم المستخدم | كلمة المرور | الدور الوظيفي | الصلاحيات والامتيازات |
|---|---|:---:|---|
| **`admin`** | `Admin@12345` | **ADMIN** | صلاحيات كاملة: لوحة التحكم، المراقبة، الكشوفات، التنبيهات، التقارير، إدارة المستخدمين، سجلات التدقيق، وتصفير التليمترية |
| **`analyst`** | `Analyst@12345` | **SECURITY_ANALYST** | صلاحيات العمليات: لوحة التحكم، المراقبة، الكشوفات، إدارة التنبيهات، والتقارير |

---

## 🚀 دليل التثبيت والتشغيل السريع

### 1. إعداد البيئة الافتراضية وتثبيت المكتبات:
```bash
# إنشاء وتفعيل البيئة الافتراضية
python3 -m venv venv
source venv/bin/activate

# تثبيت الحزم المطلوبة
pip install -r requirements.txt
```

### 2. تدريب نماذج الذكاء الاصطناعي:
```bash
python src/train_model.py
```
*يقوم بتدريب النماذج وتقييمها على مجموعة بيانات UNSW-NB15 بدقة 97.4% وحفظها في مجلد `models/`.*

### 3. تشغيل خادم لوحة التحكم الأمنية (Web Dashboard):
```bash
python src/dashboard.py
```
افتح المتصفح على: **`http://localhost:5000`** للوصول إلى منصة العمليات الأمنية.

### 4. تشغيل المراقبة الحية من سطر الأوامر (CLI):
```bash
# عرض بطاقات الشبكة المكتشفة في جهازك:
python src/realtime_monitor.py --list-interfaces

# بدء التقاط الحزم الحية من بطاقة الشبكة (مثل wlp108s0 أو eth0):
sudo ./venv/bin/python src/realtime_monitor.py --mode live --interface wlp108s0 --flow-timeout 10 --debug-capture
```

---

## 🧪 إجراءات وسيناريوهات الاختبار المعملي المصرح بها

### 1. اختبار حركة المرور الطبيعية (Normal Web / DNS):
تصفح المواقع العادية (Google, Wikipedia, GitHub) أو نفذ:
```bash
curl -s https://www.google.com > /dev/null
```
*النتيجة:* تُصنف كـ **`NORMAL`** (درجة التهديد: `0/100`، الخطورة: `LOW`، بدون أي تنبيهات كاذبة).

### 2. اختبار فحص المنافذ المصرح به (Port Scan):
```bash
nmap -sS -p 20-1000 10.251.32.71
```
*النتيجة:* رصد هجوم **`PORT_SCAN`** (درجة التهديد: `70/100`، الخطورة: `HIGH`، وإنشاء تنبيه فوري).

### 3. اختبار تخمين كلمات المرور المصرح به (SSH Brute Force):
```bash
hydra -l admin -P wordlist.txt ssh://10.251.32.71
```
*النتيجة:* رصد هجوم **`BRUTE_FORCE`** (درجة التهديد: `85/100`، الخطورة: `HIGH`، وإنشاء تنبيه فوري).

### 4. اختبار الهجوم الحجمي المصرح به (DDoS Flood):
```bash
hping3 -S --flood -p 80 10.251.32.71
```
*النتيجة:* رصد هجوم **`DDOS`** (درجة التهديد: `95/100`، الخطورة: `CRITICAL`، وإنشاء تنبيه فوري).

---

## 📁 هيكلية ملفات المشروع

```text
├── src/
│   ├── engine/                  # محرك الذكاء الاصطناعي ومعالجة الحزم اللحظي
│   │   ├── interface_discovery.py# اكتشاف بطاقات الشبكة الحية وخصائصها
│   │   ├── live_capture.py      # ملتقط الحزم المباشر وتوزيع الجلسات
│   │   ├── flow_collector.py    # جدول الجلسات ثنائي الاتجاه وحساب المعدلات
│   │   ├── feature_adapter.py   # موائم الخصائص الـ 11
│   │   └── pipeline.py          # محرك القرار متعدد المستويات وحساب درجات التهديد
│   ├── database/                # طبقة قواعد البيانات والنماذج
│   │   ├── connection.py        # مصنع الاتصال (PostgreSQL / SQLite)
│   │   ├── models.py            # نماذج المستخدمين والتدفقات والكشوفات والتنبيهات
│   │   └── seed.py              # تهيئة الحسابات الافتراضية
│   ├── auth/                    # نظام المصادقة والتحكم بالأدوار (RBAC)
│   │   ├── security.py          # أمان الجلسات وتشفير كلمات المرور
│   │   └── audit.py             # سجل التدقيق الأمني للعمليات
│   ├── routes/                  # مسارات Flask المعيارية (REST APIs والواجهات)
│   ├── data/                    # ملفات مجموعة بيانات UNSW-NB15
│   ├── models/                  # نماذج الذكاء الاصطناعي المدربة (.joblib)
│   ├── preprocessing.py         # وحدة تطبيع الخصائص وتشفير الفئات
│   ├── anomaly_model.py         # نموذج Isolation Forest لكشف الشذوذ
│   ├── classifier_model.py      # مصنف Random Forest متعدد الفئات
│   ├── realtime_monitor.py      # واجهة سطر الأوامر للمراقبة (مباشر ومحاكاة)
│   └── dashboard.py             # نقطة انطلاق خادم الويب ولوحة التحكم
├── templates/                   # قوالب واجهات الويب السيبرانية (Jinja2)
├── static/                      # ملفات التصميم CSS وجافاسكريبت والمخططات البيانية
├── schema.sql                   # مخطط قاعدة البيانات العلائقية DDL
├── requirements.txt             # المكتبات والاعتماديات البرمجية
├── إقرأني.md                   # التوثيق باللغة العربية
└── README.md                    # التوثيق باللغة الإنجليزية
```

---

## 📄 الترخيص (License)
هذا المشروع مرخص بموجب رخصة [MIT](LICENSE).
